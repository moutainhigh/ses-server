<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.redescooter.ses.web.ros.dao.specificat.SpecificatDefMapper">

    <!--新增规格自定义项-->
    <insert id="batchInsertSpecificatDef" parameterType="com.redescooter.ses.web.ros.vo.specificat.dto.SpecificDefDTO">
        insert into operation.ope_specificat_def(id, specificat_id, def_group_id,def_name, def_value,
        created_by, created_time, updated_by,updated_time)
        <foreach collection="specificDefList" item="item" open="values" separator=",">
            (#{item.id}, #{item.specificId}, #{item.specificDefGroupId},#{item.defName}, #{item.defValue}, #{item.createdBy},
            #{item.createdTime}, #{item.updatedBy}, #{item.updatedTime})
        </foreach>
    </insert>

</mapper>