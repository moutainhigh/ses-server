<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.redescooter.ses.mobile.rps.dao.scooterqc.ScooterQcServiceMapper">


    <select id="scooterQcListCount" resultType="int">
        select count(*)
        from ope_assembly_order
    </select>

    <select id="partListByIdCount" resultType="int">
        select count(*)
        from ope_assembly_b_order
    </select>

    <select id="scooterQcItemCount" resultType="int">
        select count(*)
        from ope_assembly_b_order ob
        inner join ope_assembly_order o
        on ob.assembly_id = o.id
    </select>


</mapper>